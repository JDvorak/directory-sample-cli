{
  "name": "directory-sample",
  "version": "1.0.0",
  "description": "CLI to sample from a directory",
  "main": "index.js",
  "scripts": {
    "deps": "dependency-check . && dependency-check . --extra --no-dev",
    "test": "standard && npm run deps && NODE_ENV=test node test",
    "test:cov": "standard && npm run deps && NODE_ENV=test istanbul cover test.js"
  },
  "repository": "jdvorak/directory-sample",
  "keywords": [
    ""
  ],
  "license": "MIT",
  "dependencies": {
    "alea": "0.0.9",
    "pull-abortable": "^4.1.1",
    "pull-cat": "^1.1.11",
    "pull-catch": "^1.0.0",
    "pull-cont": "^0.1.1",
    "pull-defer": "^0.2.2",
    "pull-glob": "^1.0.6",
    "pull-paramap": "^1.2.2",
    "pull-pushable": "^2.1.1",
    "pull-stream": "^3.6.1",
    "pull-stream-to-stream": "^1.3.4",
    "stream-to-pull-stream": "^1.7.2",
    "yargs": "^10.0.3"
  },
  "devDependencies": {
    "dependency-check": "^2.9.1",
    "istanbul": "^0.4.5",
    "standard": "^10.0.3",
    "tape": "^4.8.0"
  }
}
